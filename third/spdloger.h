#ifndef SPDLOGER_H
#define SPDLOGER_H
/*
class spdlogger: public spdlog::logger
{
public:
    spdlogger();

    void trace(const char* str);
    void debug(const char* str);
    void info(const char* str);
    void warn(const char* str);
    void error(const char* str);
    void critical(const char* str);
};

int l_trace(const char* str);
int l_debug(const char* str);
int l_info(const char* str);
int l_warn(const char* str);
int l_error(const char* str);
int l_critical(const char* str);
*/
#endif // SPDLOGER_H
